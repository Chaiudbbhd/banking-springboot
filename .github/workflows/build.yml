name: Build & Test Spring Boot

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: lpk2005
          MYSQL_DATABASE: banking_central
        ports:
          - 3307:3306  # map container's port 3306 to host's 3307
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set environment variable for DB URL
      run: echo "SPRING_DATASOURCE_URL=jdbc:mysql://127.0.0.1:3307/banking_central?useSSL=false&serverTimezone=UTC" >> $GITHUB_ENV

    - name: Wait for MySQL to be ready
      run: |
        for i in {30..0}; do
          if mysqladmin ping -h 127.0.0.1 -P 3307 --silent; then
            echo "MySQL is up!"
            break
          fi
          echo "Waiting for MySQL to start..."
          sleep 1
        done

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build with Maven
      run: ./mvnw clean install
